import {emptyBounds} from './impl/emptyPrimitives';
import {emptyLayoutInput} from './impl/emptyLayoutInput';
import {emptyLayout} from './impl/emptyLayout';

const empty = {
   bounds: emptyBounds,
   layoutInput: emptyLayoutInput,
   layout: emptyLayout
};

export default empty;